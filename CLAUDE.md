# Shared home-team context — single source of truth, loaded via the imports below (NOT copied here).
# refresh: git -C ~/.home-dev pull -q || gh repo clone donniedeboer/home-dev-common ~/.home-dev
# This repo IS the design-system primitive — the shared component package every home-team UI uses.
# App-specific design notes live in DESIGN.md here.
@~/.home-dev/PLATFORM.md
@~/.home-dev/CONVENTIONS.md
@~/.home-dev/ARCHITECTURE.md
